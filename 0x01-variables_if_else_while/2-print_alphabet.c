#include <stdio.h>

/**
 * main - print the letters of the alphabet in lowercase
 *
 * Description: same as above
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	for (int c = 97; c <= 122; c++)
		putchar (c);

	return (0);
}
